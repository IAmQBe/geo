[project]
name = "geo-jambot"
version = "0.1.0"
description = "Jam Bot architecture bootstrap"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
